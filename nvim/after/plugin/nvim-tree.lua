require("nvim-tree").setup({
  update_focused_file = {
    enable = true,      -- This highlights the current file in the tree
    update_root = true, -- Optional: updates the tree root to the file's directory
  },
})

